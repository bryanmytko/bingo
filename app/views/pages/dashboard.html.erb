<div class="container grid grid-cols-12 gap-4 mx-auto pt-4 w-10/12">
  <%= render partial: "game_status", locals: { game: @game } %>
  <div class="col-span-12 lg:col-span-8 xl:col-span-9 p-6 overflow-hidden rounded text-gray-400 bg-gray-800 rounded-md">
    <h3 class="text-xl font-sans mb-4 text-gray-100 uppercase">My Cards</h3>
    <div class="text-sm pb-4 leading-relaxed">
      <p>To share these with your friends, click the button to copy the link and simply send that to them.</p>
      <p>Each person to use the link will have a fresh card randomly generated for them.</p>
      <p>They don't need to even sign up or sign in!</p>
    </div>
    <%= render partial: "cards_table", locals: { cards: current_user_cards } %>
  </div>
  <div class="p-6 col-span-12 lg:col-span-4 xl:col-span-3 overflow-hidden shadow-lg bg-gray-200 rounded-md">
    <h3 class="pb-4 text-lg uppercase text-gray-900">Create a Bingo Card?</h3>
    <p class="pb-4 text-sm text-gray-700">If you haven't created a card yet start here.
    Since the cards are randomized feel free to fill out more terms than the board size
    to keep the game fun and exciting.</p>
    <a href="/cards/new" class="btn">
      <i class="material-icons">add_box</i>Create New Card!
    </a>
  </div>
  <div class="bg-amber-400 col-span-12 rounded-md shadow-lg p-6 flex mb-4">
  <% if user_signed_in? %>
    <p class="mr-2">You are currently signed in as <strong><%= current_user.email %></strong></p>
    <%# <%= link_to "Edit profile", edit_user_registration_path, :class => "navbar-link" %>
    <%= button_to "(logout)", destroy_user_session_path, method: :delete %>
  <% end %>
  </div>
</div>
