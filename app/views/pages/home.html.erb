<div class="container grid grid-cols-4 gap-4 mx-auto pt-4 w-10/12">
  <div class="col-span-3 p-6 overflow-hidden rounded text-gray-400 bg-gray-800 rounded-md">
    <h3 class="text-xl font-sans mb-4 text-gray-300 uppercase">My Cards</h3>
    <div class="text-sm pb-4 leading-relaxed">
      <p>To share these with your friends, click the button to copy the link and simply send that to them.</p>
      <p>Each person to use the link will have a fresh card randomly generated for them.</p>
      <p>They don't need to even sign up or sign in!</p>
    </div>
    <table>
      <tbody>
        <% @cards.each do |card| %>
          <tr>
            <td>
              <h4 class="pb-2 text-md text-gray-300 uppercase ">
                <%= card.title %>
              </h4>
              <div class="flex">
                <div class="mr-4">
                  <a href="/<%= card.url %>" class="btn">
                    <i class="material-icons left">content_copy</i>
                    share link
                  </a>
                </div>
                <div>
                  <ul class="flex mt-1 pipe-list">
                    <li><%= link_to "show", root_url + card.url %></li>
                    <li><%= link_to "print", "#{root_url}print/#{card.url}" %></li>
                    <li><%= link_to "edit", edit_card_path(card) %></li>
                    <li><%= link_to "delete", card_path(card), method: :delete %></li>
                  </ul>
                </div>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="p-6 col-span-1 overflow-hidden shadow-lg bg-gray-100 rounded-md">
    <h3 class="pb-4 text-xl">Create a Bingo Card?</h3>
    <p class="pb-4 text-sm">If you haven't created a card yet start here. You will be asked to fill out at least 75 terms to keep the game fun and exciting.</p>
    <a href="/cards/new" class="btn">
      <i class="material-icons">add_box</i>Create Bingo Card!
    </a>
  </div>
  <div class="bg-yellow-200 col-span-4 rounded-md shadow-lg p-6 flex">
  <% if user_signed_in? %>
    <p class="mr-2">You are currently signed in as <strong><%= current_user.email %></strong></p>
    <%# <%= link_to "Edit profile", edit_user_registration_path, :class => "navbar-link" %>
    <%= button_to "(logout)", destroy_user_session_path, method: :delete %>
    <!-- @TODO should show Card.all with a button for creating new, not rendering the form -->
  <% end %>
  </div>
</div>
